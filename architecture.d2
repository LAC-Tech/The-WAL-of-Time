title: |md
  # Interlog
| {near: top-center}

sm: State Machine {
  shape: class
  -node_id
  -local_fd
  -remote_fds
  -buffered_reqs
  # transition(OsRes): "Os::Req[]"
}

linux: {
  shape: class
  -io_uring
  # wait_for_res(): Os::Res
  # "submit_reqs(Os::Req[])": usize
}

sm -> linux: "submit these SQEs"
linux -> sm: "wait for a CQE"
